export const LogInInput = ({ placeholder }: { placeholder: string }) => {
  return (
    <input
      type="text"
      placeholder={placeholder}
      className="flex px-2 py-3 items-center rounded border solid border-[#E4E4E7] bg-[#FFF]"
    />
  );
};
